/**
 * Session / token management subpath.
 *
 * Reserved contract surface for admin session and token management. Currently
 * empty — session logic is owned by the payment-pc-core package. Aligns with
 * `CORE_EXPORT_SUBPATHS` in `sdkwork-specs/tools/lib/app-composition.mjs`.
 */

export {};
